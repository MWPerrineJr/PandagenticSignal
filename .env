# Committed defaults for hosted builds (Lovable has no env-var UI, so Vite reads this file).
# Every value here is public: the API is CORS/rate-limited and the Supabase key is the
# browser-safe *publishable* key (row-level security is the real boundary).
# Local overrides go in .env.local (gitignored), e.g. VITE_API_URL=http://localhost:8000
VITE_API_URL=https://stock-tool-api-qg9s.onrender.com
VITE_SUPABASE_URL=https://agumrmsaeblcldcygajl.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_KsplVtuYzzfcbGIcV8ROAQ_ON-5b221
